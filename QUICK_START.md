# 🚀 快速启动指南

## 第一步：安装依赖

```bash
cd Web
pnpm install
```

> 如果没有安装 pnpm，请先运行：`npm install -g pnpm`

## 第二步：启动开发服务器

```bash
pnpm dev
```

浏览器访问：**http://localhost:3000**

## 第三步：查看效果

- **首页**：http://localhost:3000
- **赛事详情**：http://localhost:3000/tournament

## 第四步：构建部署

```bash
pnpm build
```

静态文件输出到 `out/` 目录

---

## 📂 项目文件说明

```
GuDaWebPage/
├── PRD/                    # 产品需求文档
├── Tech/                   # 技术设计文档
├── Web/                    # 网站源码 👈 主要工作目录
│   ├── src/
│   │   ├── data/          # 📝 在这里更新数据
│   │   ├── components/    # React 组件
│   │   └── app/           # 页面路由
│   ├── package.json
│   └── next.config.js
└── PROJECT_SUMMARY.md      # 项目总结
```

---

## 🎯 常用命令

| 命令 | 说明 |
|:---|:---|
| `pnpm dev` | 启动开发服务器 |
| `pnpm build` | 构建生产版本 |
| `pnpm start` | 启动生产服务器（需先 build） |
| `pnpm lint` | 代码检查 |

---

## 📝 如何更新数据

### 1. 更新战队信息

编辑 `Web/src/data/teams.ts`

### 2. 更新比赛数据

编辑 `Web/src/data/matches.ts`

### 3. 更新积分榜

编辑 `Web/src/data/standings.ts`

修改后保存，开发服务器会自动刷新。

---

## 🐛 常见问题

### Q: pnpm: command not found

**解决**：
```bash
npm install -g pnpm
```

### Q: 端口被占用

**解决**：
```bash
pnpm dev -p 3001  # 使用其他端口
```

### Q: 修改代码后没反应

**解决**：
- 检查是否保存文件
- 刷新浏览器（Cmd+R / Ctrl+R）
- 重启开发服务器

---

## 📚 更多文档

- **开发指南**：`Web/DEVELOPMENT.md`
- **PRD 文档**：`PRD/GuDa_Club_Website_PRD.md`
- **技术文档**：`Tech/Technical_Design_Document.md`
- **项目总结**：`PROJECT_SUMMARY.md`

---

**祝开发顺利！** 🎉

